# Ouroboros Reference Overview

## Role In Coflow

coflow's plan and exec strategy is inspired by the local Ouroboros project.
Ouroboros provides the source concept for specification-first planning, ambiguity-gated handoff, structured execution, and post-work evaluation.

This repository is not a full Ouroboros port.
coflow adapts the concept for Codex App and Codex CLI users by putting the durable control surface into a repo-local `co.py` CLI and Codex skill instructions.
When the two designs differ, prefer coflow's current runtime contract unless the task is explicitly to make coflow closer to Ouroboros.

## Source Path Convention

External Ouroboros repository root: `/Users/user1/projects/ouroboros`.

Ouroboros source references in this reference tier use paths relative to that external repository root, for example `README.md:39` or `src/ouroboros/execution/double_diamond.py:1`.
Coflow source references still use paths relative to the coflow repository root.

The file name `OUROBOROUS.md` keeps the user-facing spelling requested for this coflow reference tier.

## Design Philosophy

Ouroboros describes itself as a specification-first workflow engine for AI coding agents.
Its README says the product sits between the user and Codex CLI and replaces ad-hoc prompting with `interview -> crystallize -> execute -> evaluate -> evolve`.

Important source locations:

- `README.md:39`: one-line Codex CLI product promise.
- `README.md:41`: structured workflow statement.
- `README.md:47`: core claim that AI coding fails at input clarity.
- `README.md:51`: vague prompts are handled by Socratic interview.
- `README.md:52`: immutable seed spec locks intent.
- `README.md:53`: manual QA is replaced by a 3-stage evaluation gate.
- `README.md:147`: Ouroboros loop is the architecture, not decoration.
- `README.md:150`: `Interview -> Seed -> Execute -> Evaluate` loop.
- `README.md:155`: evaluation output feeds the next generation.
- `README.md:271`: philosophy section starts.
- `README.md:279`: ontological questions are treated as the deeper requirement layer.
- `README.md:288`: ontological questions are framed as practical, not decorative.
- `README.md:290`: Double Diamond is the architecture bridge.
- `README.md:304`: first diamond is Socratic, second is pragmatic.
- `README.md:375`: two mathematical gates: ambiguity <= 0.2, similarity >= 0.95.

## Command Surfaces

Ouroboros has two command surfaces, and they are not one-to-one equivalents.
This matters for coflow because coflow intentionally avoids a broad MCP/plugin surface and instead makes `co.py` the mechanical manager.

Important source locations:

- `README.md:187`: `ooo <cmd>` inside AI coding agent sessions and `ouroboros` in terminal.
- `README.md:191`: `ooo setup` and `ouroboros setup`.
- `README.md:192`: `ooo interview` and `ouroboros init start`.
- `README.md:193`: `ooo seed` is generated by interview.
- `README.md:194`: `ooo run` maps to `ouroboros run seed.yaml`.
- `README.md:195`: `ooo evaluate` is via MCP.
- `README.md:209`: not all skills have direct CLI equivalents.
- `llms-full.txt:22`: command surfaces are explicitly not one-to-one.
- `llms-full.txt:26`: `ooo` skill surface.
- `llms-full.txt:43`: Typer CLI surface.
- `pyproject.toml:32`: package script exposes `ouroboros`.
- `pyproject.toml:33`: CLI entrypoint is `ouroboros.cli.main:app`.

## Architecture Map

Ouroboros maps its core runtime under `src/ouroboros/`.
The parts most relevant to coflow are `bigbang`, `execution`, `evaluation`, `orchestrator`, `providers`, `mcp`, `agents`, `commands`, and `skills`.

Important source locations:

- `README.md:238`: source layout block starts.
- `README.md:240`: `bigbang` handles interview and ambiguity scoring.
- `README.md:242`: `execution` handles Double Diamond and AC decomposition.
- `README.md:243`: `evaluation` handles mechanical, semantic, and consensus evaluation.
- `README.md:248`: `orchestrator` is the runtime abstraction layer for Codex execution.
- `README.md:250`: providers include Codex-backed completion adapters.
- `README.md:251`: MCP client/server integration.
- `README.md:254`: Typer CLI.
- `llms-full.txt:91`: six phases are listed.
- `llms-full.txt:93`: Big Bang crystallizes requirements.
- `llms-full.txt:95`: Double Diamond decomposes and executes tasks.
- `llms-full.txt:97`: Evaluation verifies outputs in three stages.

## Coflow Adaptation Boundary

Use Ouroboros as a concept source, not as a strict compatibility target.

- Ouroboros has interview, seed, execute, evaluate, evolve, MCP tools, plugin skills, event sourcing, runtime adapters, and optional persistent loops.
- coflow currently ships two Codex skills: `skills/coplan` and `skills/coexec`.
- coflow's durable state is `.agents/plan/{plan-id}` and the mechanical manager is `skills/coplan/scripts/co.py`.
- coflow's root agent should stay thinner than the historical Ouroboros skill surface: run `co.py flow`, parse YAML, and perform only the returned `root_action`.
- If a design idea from Ouroboros is adopted, prefer implementing it as a `co.py` command, validation rule, stdout field, or bundle schema change instead of only adding prose to `SKILL.md`.

## Detailed References

- Plan and seed concepts: `.agents/knowledge/references/OUROBOROS-PLAN.md`.
- Execution and runtime concepts: `.agents/knowledge/references/OUROBOROS-EXEC.md`.
- Evaluation and verification concepts: `.agents/knowledge/references/OUROBOROS-EVAL.md`.
